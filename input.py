class Input(object):
	"""docstring for Input"""
	def __init__(self, player, layer, name, side, pos):
		self.player = player
		self.layer = layer
		self.name = name
		self.side = side
		self.pos = pos
		